server:
  port: 8082

spring:
  application:
    name: backlog-service
  datasource:
    url: jdbc:postgresql://localhost:5432/backlog_db
    username: postgres
    password: ${BACKLOG_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

game-service:
  base-url: ${GAME_SERVICE_URL:http://localhost:8081}